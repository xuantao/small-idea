
enum Enum1;

int g_i = 11;
float g_f = 12;
bool g_bf = false;
bool g_bt = true;
string g_str = "yaner";

enum Enum0
{
};

enum Enum1
{
	_bool,
	_int,
	_string,
};

enum Enum1
{
	_bool,
	_int = 1,
	_string
};
